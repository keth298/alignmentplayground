{
  "name": "alignment-playground",
  "version": "0.1.0",
  "private": true,
  "workspaces": ["frontend", "packages/*"],
  "scripts": {
    "dev": "concurrently \"npm run dev --workspace=frontend\"",
    "build": "npm run build --workspace=frontend",
    "lint": "npm run lint --workspace=frontend"
  },
  "devDependencies": {
    "concurrently": "^8.0.0"
  }
}
